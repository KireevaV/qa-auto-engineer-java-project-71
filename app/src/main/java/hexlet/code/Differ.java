package hexlet.code;

public class Differ {
    public static String generate(String filePath1, String filePath2, String format) {
        // Пока возвращаем заглушку с учётом формата
        return "Comparison result in " + format + " format";
    }
}
